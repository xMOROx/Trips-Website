/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DestinationCountryPipe } from './destinationCountry.pipe';

describe('Pipe: DestinationCountrye', () => {
  it('create an instance', () => {
    let pipe = new DestinationCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
