/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StarsPipe } from './stars.pipe';

describe('Pipe: Starse', () => {
  it('create an instance', () => {
    let pipe = new StarsPipe();
    expect(pipe).toBeTruthy();
  });
});
