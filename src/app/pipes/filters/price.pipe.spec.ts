/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PricePipe } from './price.pipe';

describe('Pipe: Pricee', () => {
  it('create an instance', () => {
    let pipe = new PricePipe();
    expect(pipe).toBeTruthy();
  });
});
